apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: pv-www-claim
    namespace: demo-webapp
spec:
    storageClassName: gp2
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 100Mi
